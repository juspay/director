# Ralph Progress Log
Started: 2026-03-26
PRD: Director Pipeline Improvements (32 user stories)

## Codebase Patterns
- All pipeline code is in video-production/library/ (organized by domain: voiceover, music, sfx, mixing, scoring, rendering, pipeline, remotion)
- Python files have origin comments: `# Origin: {version} — extracted to library on 2026-03-23`
- Remotion components have JSDoc headers with @component, @origin, @description tags
- Requirements go in library/pipeline/requirements.txt
- New directories may need to be created (e.g., library/distribution/)
- The pipeline uses async Python with asyncio, tenacity for retries
- Audio processing uses pydub and numpy/scipy currently
- Scoring uses Google Vertex AI / Gemini 2.5 Pro
- All files are code-only (no binary assets tracked in git)
- EncodingPreset.ffmpeg_video_args() in pipeline_config.py is the central point for all FFmpeg encoding flags — modify there for global encoding changes
- assembler.py has some hardcoded ffmpeg commands (e.g., color grading) that bypass EncodingPreset — check both files when changing encoding
---

## 2026-03-26 - US-001
- Added `-tune animation` to all H.264 encoding via EncodingPreset.ffmpeg_video_args()
- Also added to hardcoded color grading command in assembler.py
- Files changed: pipeline_config.py, assembler.py
- **Learnings for future iterations:**
  - ffmpeg_video_args() is the main encoding flag generator, but assembler.py has a hardcoded libx264 call in apply_color_grade() — always check both
  - The flag auto-applies only when video_codec == "libx264" so it's future-safe for AV1/HEVC presets
---

## 2026-03-26 - US-002
- Upgraded Whisper model_size from "large-v2" to "large-v3-turbo" in caption_burner.py
- Updated docstring and comment to reflect new model specs
- Files changed: caption_burner.py
- **Learnings for future iterations:**
  - caption_burner.py has a single model_size default parameter — change it there
  - The model name must match faster-whisper's CTranslate2 conversion names on HuggingFace
---

## 2026-03-26 - US-003
- Changed temperature from 0.1 to 0.0 in gemini_video_scorer.py (default param + call site)
- Files changed: gemini_video_scorer.py
- **Learnings for future iterations:**
  - The analyze() function has both a default param AND an explicit call-site value — must change both
  - The scoring prompt is stored in SCORING_PROMPT_V8 constant
---

## 2026-03-26 - US-005
- Changed FontSize from 22 to 32 in ASS_STYLE constant
- Files changed: caption_burner.py
- **Learnings for future iterations:**
  - ASS_STYLE is a single string constant at the top of caption_burner.py — all caption styling is there
  - For Shorts/TikTok vertical format, 44-48pt would be needed (separate preset)
---

## 2026-03-26 - US-004
- Refactored scoring prompt into a template with shuffleable dimension blocks
- Added SCORING_DIMENSIONS data, DIMENSION_BODIES dict, build_scoring_prompt(run_seed)
- Extraction regex now uses [A-G] instead of fixed letters
- Each run gets a different dimension order via seeded Random
- Files changed: gemini_video_scorer.py
- **Learnings for future iterations:**
  - The prompt was renamed from SCORING_PROMPT_V8 to SCORING_PROMPT_V8_TEMPLATE (uses .format())
  - Any code referencing SCORING_PROMPT_V8 directly needs to use build_scoring_prompt() instead
  - The extract_dimension_scores() regex uses dimension NAMES not letter prefixes
---

## 2026-03-26 - US-006
- Added measure_loudness() function using pyloudnorm (ITU-R BS.1770-4)
- Called after both pydub and scipy WAV export paths
- Measures integrated LUFS + true peak dBTP with configurable targets
- Gracefully handles missing pyloudnorm via HAS_PYLOUDNORM flag
- Added pyloudnorm to requirements.txt
- Files changed: vad_mixer.py, requirements.txt
- **Learnings for future iterations:**
  - vad_mixer.py has TWO export paths: pydub (line ~373) and scipy fallback (line ~454) — changes to post-export must hit both
  - pyloudnorm needs float64 data in [-1,1] with shape (samples, channels) — int16 WAV data must be converted
  - TARGET_LUFS and LUFS_TOLERANCE are module-level constants for easy tuning
---

## 2026-03-26 - US-007
- Replaced RMS-based VAD with Silero VAD (torch.hub snakers4/silero-vad)
- Added _detect_voice_silero() with 16kHz resampling and timestamp mapping
- Preserved _detect_voice_rms() as fallback when torch not installed
- Added torch>=2.0 to requirements.txt (optional dependency)
- Files changed: vad_mixer.py, requirements.txt
- **Learnings for future iterations:**
  - Silero VAD needs 16kHz audio — resample with scipy.signal.resample before processing
  - get_speech_timestamps() returns list of dicts with 'start'/'end' keys in samples at 16kHz
  - torch.hub.load caches the model after first download (~4MB)
  - The existing create_duck_envelope() function is agnostic to the VAD source — any 0/1 activity array works
---

## 2026-03-26 - US-008
- Converted NARRATION_SSML to NARRATION_AUDIOTAG with ElevenLabs v3 audio tags
- [pause] replaces <break time="0.3-0.5s"/>, [long pause] replaces <break time="0.6-0.8s"/>
- [rushed] replaces <prosody rate="105%">
- Renamed use_ssml→use_audiotags, --ssml→--audiotags throughout
- Files changed: genetic_optimizer.py
- **Learnings for future iterations:**
  - ElevenLabs v3 audio tags: [pause], [long pause], [rushed], [excited], [whispers], [sighs]
  - v3 does NOT support SSML — tags are read as literal text
  - chirp3_ssml.py and edgetts_ssml.py still use SSML legitimately (Google/Edge support it)
  - Only ElevenLabs files need audio tags; other TTS providers keep SSML
---

## 2026-03-26 - US-009
- Added av1_final and av1_web presets using libsvtav1
- Refactored ffmpeg_video_args() to be codec-aware (libx264 vs libsvtav1 vs generic)
- Added extra_args field to EncodingPreset for codec-specific flags
- AV1 uses -svtav1-params for keyint, tune, overlays, scene change detection
- Files changed: pipeline_config.py
- **Learnings for future iterations:**
  - SVT-AV1 preset numbers are strings ("6"), not ints — passed as -preset "6"
  - AV1 CRF 30 ≈ H.264 CRF 18 quality-wise
  - 10-bit (yuv420p10le) improves gradient banding in motion graphics
  - libopus is the natural audio codec pairing for AV1/WebM containers
  - extra_args field defaults to None, handled with `if self.extra_args` check
---

## 2026-03-26 - US-010
- Added draft_hw and preview_hw presets using h264_videotoolbox
- Added video_bitrate field for hardware encoders (no CRF support)
- Added h264_videotoolbox branch in ffmpeg_video_args()
- Added is_videotoolbox_available() platform detection (Darwin + ffmpeg check)
- Added get_preset(name, prefer_hw=True) for automatic HW fallback
- Files changed: pipeline_config.py
- **Learnings for future iterations:**
  - VideoToolbox does NOT support CRF — must use -b:v bitrate mode
  - is_videotoolbox_available() checks both platform.system() == "Darwin" AND ffmpeg encoder list
  - get_preset() maps "draft" → "draft_hw" automatically when prefer_hw=True
  - PRESETS dict now has 9 entries: final, preview, draft, web, social, av1_final, av1_web, draft_hw, preview_hw
---

## 2026-03-26 - US-011
- Added Pedalboard-backed soft_compress() and brick_wall_limiter() with NumPy fallback
- Pedalboard uses C++/JUCE backend (~300x faster than sample-by-sample loops)
- Expects float32 with shape (channels, samples) — conversion handled in wrapper
- Added pedalboard>=0.9 to requirements.txt
- Files changed: vad_mixer.py, requirements.txt
- **Learnings for future iterations:**
  - Pedalboard expects float32 ndarray with shape (1, num_samples) for mono — reshape(1, -1)
  - pedalboard.Compressor params: threshold_db, ratio, attack_ms, release_ms
  - pedalboard.Limiter params: threshold_db (positive value, e.g., -1.0)
  - HAS_PEDALBOARD flag gates all Pedalboard usage — safe to import even without it
  - US-020 (frequency-aware ducking) depends on this — uses Pedalboard filters for band splitting
---

## 2026-03-26 - US-012
- Added context caching: after run 1, video is cached for runs 2-3 (90% token savings)
- analyze() accepts optional cached_content parameter (cache resource name)
- Cache created via client.caches.create() with 900s TTL
- Graceful fallback if caching API unavailable
- Files changed: gemini_video_scorer.py
- **Learnings for future iterations:**
  - Gemini caching API: client.caches.create(model=, contents=, config=CreateCachedContentConfig(ttl=))
  - Cache is passed as cached_content kwarg to generate_content()
  - TTL of 900s (15 min) is enough for 3 runs; storage cost $4.50/1M tokens/hr
  - Cache only created when num_runs > 1 (single runs skip caching)
  - The scoring loop structure changed: run 1 is now outside the `else` branch
---
