# Verify window 28

Based on a forensic re-examination of the provided frames, here is a list of corrections for the prior description.

*   The description of the "JUSPAY hyperswitch" logo is incorrect. The text "JUSPAY hyperswitch" appears on a single line, not on two separate lines as stated ("'JUSPAY' on the top line and 'hyperswitch' on the bottom line").
*   Consequently, the "VERBATIM Text Labels" in the summary are also incorrect. It should list "JUSPAY hyperswitch" as a single label and not provide separate vertical coordinates for "JUSPAY" and "hyperswitch".
*   The description omits that all three tiles have a clear, transparent border around their colored area, which contributes to their "sticker-like" appearance.
*   There is a minor inconsistency in the reported horizontal position for the Recurly tile. The frame-by-frame analysis lists it at `(0.33, 0.26)`, while the summary lists it at `(0.35, 0.26)`.
