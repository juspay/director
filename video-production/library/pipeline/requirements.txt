faster-whisper>=1.0.0
ffmpeg-quality-metrics>=3.0  # VMAF/SSIM/PSNR computation
httpx>=0.27.0
jiwer>=3.0  # Word Error Rate computation for caption validation
moviepy>=2.0.0
mutagen>=1.47.0
openai>=1.0  # OpenAI gpt-4o-mini-tts voiceover provider
optuna>=3.0  # Bayesian optimization for TTS parameter search
pedalboard>=0.9  # Spotify Pedalboard — studio-quality audio effects (optional)
pyloudnorm>=0.1
speechmos>=1.0  # UTMOSv2 MOS prediction for voiceover quality (optional)
python-dotenv>=1.0.0
torch>=2.0  # for Silero VAD (optional — mixer falls back to RMS if missing)
tenacity>=8.2.0
whisperx>=3.0  # WhisperX — wav2vec2 forced alignment for precise word timestamps (optional)
